on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

name: release-please-publish

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      releases_created: ${{ steps.release.outputs.releases_created }}
      paths_released: ${{ steps.release.outputs.paths_released }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.NOMBOT_CLASSIC_PERSONAL_ACCESS_TOKEN }}
          config-file: .github/release-please-config.json
          manifest-file: .github/release-please-manifest.json

  publish-python:
    needs: release-please
    if: ${{ needs.release-please.outputs.releases_created == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        path: ${{ fromJSON(needs.release-please.outputs.paths_released) }}
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.8.x"
          enable-cache: false
      - name: Resolve package name
        id: pkg
        env:
          RELEASED_PATH: ${{ matrix.path }}
        run: |
          set -euo pipefail
          if [ "$RELEASED_PATH" = "." ]; then
            echo "name=instro" >> "$GITHUB_OUTPUT"
          else
            echo "name=$(basename "$RELEASED_PATH")" >> "$GITHUB_OUTPUT"
          fi
      - name: Detect Python package layout
        id: layout
        env:
          RELEASED_PATH: ${{ matrix.path }}
        run: |
          set -euo pipefail
          # Publish any released path that ships a Python distribution. Pure-Rust
          # crates (Cargo.toml but no pyproject.toml) are handled by publish-crates.
          # Mixed Rust/Python packages have both files and build via maturin under uv.
          if [ "$RELEASED_PATH" = "." ] || [ -f "$RELEASED_PATH/pyproject.toml" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
          else
            echo "publish=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -f "$RELEASED_PATH/Cargo.toml" ] && [ -f "$RELEASED_PATH/pyproject.toml" ]; then
            echo "mixed=true" >> "$GITHUB_OUTPUT"
            echo "$RELEASED_PATH is a mixed Rust/Python (maturin) package" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "mixed=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Build wheel and sdist
        if: ${{ steps.layout.outputs.publish == 'true' }}
        env:
          PACKAGE_NAME: ${{ steps.pkg.outputs.name }}
        run: uv build --package "$PACKAGE_NAME" --out-dir dist
      - name: Publish to PyPI
        if: ${{ steps.layout.outputs.publish == 'true' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  publish-crates:
    needs: release-please
    if: ${{ needs.release-please.outputs.releases_created == 'true' }}
    runs-on: ubuntu-latest
    environment: crates-io-release

    permissions:
      contents: read
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        path: ${{ fromJSON(needs.release-please.outputs.paths_released) }}

    steps:
      - name: Filter publishable crates
        id: crate-pkg
        run: |
          if [[ "${{ matrix.path }}" == crates/* ]]; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
            echo "name=$(basename "${{ matrix.path }}")" >> "$GITHUB_OUTPUT"
          else
            echo "skip=true" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v4
        if: ${{ steps.crate-pkg.outputs.skip == 'false' }}

      - name: Install Rust
        if: ${{ steps.crate-pkg.outputs.skip == 'false' }}
        run: rustup show

      - name: Authenticate to crates.io
        if: ${{ steps.crate-pkg.outputs.skip == 'false' }}
        uses: rust-lang/crates-io-auth-action@v1
        id: auth

      - name: Publish crates to crates.io
        if: ${{ steps.crate-pkg.outputs.skip == 'false' }}
        run: cargo publish --locked --package "${{ steps.crate-pkg.outputs.name }}"
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
